#include<stdio.h>
int n,m;
int a[60],b[60],p[100][100][60];
void read(){
	void schedule(){
		in x,y,k;
		for(x = 0;x <= m;x++){
			for(y = 0;y < m;y++){
				if(x - a[k-1] >= 0)	
					p[x][y][k] = p[x-q[k-1][u][k-1]] 
			}
		}
	}
}
